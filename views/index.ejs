<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <title>ToDo一覧ページ</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="p-4 bg-light">
    <div class="container">
      <h1 class="text-primary mb-3">📋 ToDo一覧ページ</h1>
      <a href="/new" class="btn btn-success mb-3">＋ 新しいToDoを追加</a>

      <table class="table table-striped align-middle">
        <thead>
          <tr>
            <th>ID</th>
            <th>内容</th>
          </tr>
        </thead>
        <tbody>
          <% if (todos.length === 0) { %>
          <tr>
            <td colspan="2" class="text-muted">登録されたToDoはありません。</td>
          </tr>
          <% } else { %>
          <% todos.forEach(todo => { %>
          <tr>
            <td><%= todo.id %></td>
            <td><%= todo.title %></td>
          </tr>
          <% }) %>
          <% } %>
        </tbody>
      </table>

      <a href="/edit" class="btn btn-warning">更新ページへ</a>
      <a href="/delete" class="btn btn-danger ms-2">削除ページへ</a>
    </div>
  </body>
</html>