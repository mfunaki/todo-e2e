<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ToDo一覧</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="p-4 bg-light">
    <div class="container">
      <h1 class="text-primary mb-4">📋 ToDo一覧</h1>
      
      <div class="mb-4">
        <a href="/new" class="btn btn-success">➕ 新規登録</a>
      </div>

      <% if (todos.length === 0) { %>
        <div class="alert alert-info">
          ToDoがありません。新規登録から追加してください。
        </div>
      <% } else { %>
        <div class="list-group">
          <% todos.forEach(todo => { %>
            <div class="list-group-item <%= todo.completed ? 'list-group-item-secondary' : '' %>">
              <div class="d-flex justify-content-between align-items-start">
                <div class="flex-grow-1">
                  <h5 class="mb-1 <%= todo.completed ? 'text-decoration-line-through text-muted' : '' %>">
                    <%= todo.title %>
                  </h5>
                  <% if (todo.description) { %>
                    <p class="mb-1 <%= todo.completed ? 'text-muted' : '' %>">
                      <%= todo.description %>
                    </p>
                  <% } %>
                  <small class="<%= todo.completed ? 'text-muted' : 'text-secondary' %>">
                    状態: <%= todo.completed ? '✅ 完了' : '⏳ 未完了' %>
                  </small>
                </div>
                <div class="btn-group ms-3" role="group">
                  <a href="/edit/<%= todo.id %>" class="btn btn-sm btn-warning">✏️ 編集</a>
                  <a href="/delete/<%= todo.id %>" class="btn btn-sm btn-danger">🗑️ 削除</a>
                </div>
              </div>
            </div>
          <% }) %>
        </div>
      <% } %>

      <div class="mt-4">
        <p class="text-muted">
          合計: <%= todos.length %>件 
          (完了: <%= todos.filter(t => t.completed).length %>件、
          未完了: <%= todos.filter(t => !t.completed).length %>件)
        </p>
      </div>
    </div>
  </body>
</html>
